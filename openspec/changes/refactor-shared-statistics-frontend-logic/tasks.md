## 1. 共用模組建立與安全注入

- [x] 1.1 建立 `statisticsShared.html` 並封裝統計狀態、摘要載入、群類明細載入與渲染邏輯
- [x] 1.2 將共用模組注入點固定在 `index.html` 與 `teacherView.html` 的 `<script>` 區塊，禁止放入 `<style>`
- [x] 1.3 建立單一初始化呼叫介面（例如 `initProgressiveStatisticsTab()`），定義必要 DOM 前置條件

## 2. 雙頁整合與事件生命週期收斂

- [x] 2.1 移除 `index.html` 內重複統計腳本，改為共用模組初始化
- [x] 2.2 移除 `teacherView.html` 內重複統計腳本，改為共用模組初始化
- [x] 2.3 移除舊有 tab 監聽與直接呼叫內部函式邏輯，避免重複綁定與作用域錯誤

## 3. 回歸驗證與文件同步

- [ ] 3.1 手動驗證學生端：首次載入、群類切換、展開更多、錯誤提示顯示
- [ ] 3.2 手動驗證老師端：首次載入、群類切換、展開更多、錯誤提示顯示
- [x] 3.3 更新 README 共用模組說明與回退策略，並記錄已知限制（`statisticsTemplate.html` 仍為舊流程）
- [x] 3.4 準備回退步驟（回復雙檔內嵌統計腳本）並於 PR 描述附上操作指引