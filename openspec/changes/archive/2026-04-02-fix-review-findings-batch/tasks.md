## 0. 前置確認（工作表欄位）

- [x] 0.0 先確認欄位對照文件：`worksheet-columns.md`
- [x] 0.1 盤點並確認「考生志願列表」欄位名稱：是否參加集體報名、報考群(類)代碼、報考群(類)名稱、志願1代碼（及相容鍵 志願1校系代碼）、志願1校系名稱
- [x] 0.2 盤點並確認「參數設定」為鍵值結構（A=參數名稱、B=參數值、C=參數說明），不可用一般第一列欄位邏輯處理
- [x] 0.3 將欄位確認結果記錄於實作紀錄，若有差異先更新對應安全 fallback
- [x] 0.4 先閱讀欄位差異報告：`worksheet-column-diff-report.md`，優先處理「志願1校系代碼 vs 志願1代碼」差異

## 1. 統計正確性修補（第一優先）

- [x] 1.1 修正 `statistics.gs` 志願欄位索引計算，統一使用單一路徑取值並移除混用邏輯
- [x] 1.2 建立統計結果驗證案例（代碼欄位模式、名稱欄位回退模式）並人工比對輸出
- [x] 1.3 提交 `fix(statistics): 修正志願索引邏輯`（高頻提交批次 1）

## 2. 前端安全輸出與錯誤顯示修補

- [x] 2.1 調整 `index.html` 中設定值與錯誤訊息顯示路徑，避免不安全 `innerHTML` 注入
- [x] 2.2 調整 `teacherView.html` 中導覽列與頁尾動態內容輸出為安全策略
- [x] 2.3 修正 `index.html` 統計錯誤訊息區塊無效 HTML 標籤，確保錯誤可見
- [x] 2.4 執行前端關鍵區塊回歸檢查（導覽列、頁尾、錯誤區塊）
- [x] 2.5 提交 `fix(frontend): 強化動態輸出安全與錯誤渲染`（高頻提交批次 2）

## 3. 伺服端穩定性修補

- [x] 3.1 在 `main.gs` 將隱含全域 `record` 改為區域宣告
- [x] 3.2 在 `main.gs` 的 `doGet` 補上未知使用者角色 fallback 回應
- [x] 3.3 執行 `doGet` / `doPost` 路徑檢查，確認不再出現空回應或跨呼叫狀態污染
- [x] 3.4 提交 `fix(main): 修正作用域與回傳穩定性`（高頻提交批次 3）

## 4. 效能優化與快取恢復

- [x] 4.1 調整 `retrieveData.gs` 的學生資料讀取流程，避免每次全表掃描
- [x] 4.2 在 `statistics.gs` 恢復短時快取，並與既有清除快取入口整合
- [x] 4.3 執行效能驗證（重複查詢統計、單一使用者資料讀取）並記錄觀察結果
- [x] 4.4 提交 `perf(data): 優化資料讀取並恢復統計快取`（高頻提交批次 4）

## 5. 收斂與發佈前確認

- [x] 5.1 檢查四個高頻提交皆可獨立回滾且互不衝突
- [x] 5.2 逐項對照 `specs/review-findings-remediation/spec.md` 完成需求覆蓋確認
- [x] 5.3 更新變更說明與驗證紀錄，完成 `/opsx:apply` 實作階段

## 6. 驗收標準（Acceptance Criteria）

- [x] 6.1 統計邏輯驗收：在代碼模式與名稱回退模式下，統計結果皆與人工抽查一致
- [x] 6.2 安全輸出驗收：`index.html` 與 `teacherView.html` 高風險動態文字不再直接注入不可信 HTML
- [x] 6.3 錯誤顯示驗收：統計錯誤區塊可正常渲染，且錯誤文字可見且不執行腳本
- [x] 6.4 穩定性驗收：`doGet` 未知角色有明確回應，`doPost` 無隱含全域狀態
- [x] 6.5 效能驗收：資料讀取不再依賴每次全表掃描，統計快取可命中且可被管理功能清除
- [x] 6.6 交付驗收：至少四個高頻提交，且每個提交可獨立回滾與驗證
